# 🗺️ 游戏客户端/引擎底层内功终极天赋树（打勾自检清单）

## 🖥️ 境界一：筑基 —— 计算机组成原理与硬件榨取（CO）
**师兄批注**：做游戏引擎，本质上就是和 CPU、内存条做物理对抗。你不懂硬件怎么拿数据，你的代码写得再优雅也是一坨慢代码！

- [ ] CPU 高速缓存与 Cache Line（绝对核心）：L1/L2/L3 缓存的物理层级。为什么 CPU 每次去内存拿数据要“买一赠十”（拉取 64 字节的 Cache Line）？
- [ ] 缓存未命中（Cache Miss）灾难：为什么二维数组【按列遍历】比【按行遍历】慢 10 倍？面向数据设计（DOD / ECS 架构）是如何利用物理连续内存拯救游戏帧率的？
- [ ] 浮点数陷阱（IEEE 754 标准）：十进制小数转换二进制为什么会产生“无限循环”？float 的 23 位尾数强行截断会导致什么精度灾难？为什么游戏里绝对不能写 if (a == 0.1)？
- [ ] 大小端字节序（Endianness）：大端（高位存低地址，网络/人类习惯）与小端（低位存低地址，x86 CPU 习惯）的物理存储差异。
- [ ] SIMD（单指令多数据流）：为什么你的软光栅渲染器里，算 4 个顶点的矩阵乘法不需要写 4 次，而是用硬件指令一次性轰炸完？

## 🧠 境界二：结丹 —— 操作系统内存魔法（OS 内存篇）
**师兄批注**：C++ 程序员每天都在 new/delete，但 OS 给你的内存地址全是他妈的幻觉！

- [ ] 虚拟内存的弥天大谎：为什么每个进程都以为自己独占 4GB 内存？MMU（内存管理单元）和页表（Page Table）是如何做“幻觉地址”到“物理地址”的映射的？
- [ ] 缺页中断（Page Fault）：当你访问一个被 OS 偷偷塞进硬盘的虚拟内存时，CPU 是如何封锁现场，让 OS 去硬盘里把数据捞回内存条的？（游戏过图卡顿的元凶之一）。
- [ ] 内存碎片（Fragmentation）：内部碎片（系统按页分配给多了）与外部碎片（内存东一块西一块凑不出一个大块）。游戏引擎为什么要自己写内存池来规避外部碎片？

## ⚙️ 境界三：化神 —— 操作系统调度与并发（OS 进程/线程篇）
**师兄批注**：游戏主线程（渲染逻辑）绝对不能被阻塞！这一块懂了，你就是解决游戏“死亡螺旋”卡顿的神！

- [ ] 进程与线程的物理隔离：进程是“资源分配”的单位（有独立页表和内存），线程是“CPU 调度”的单位（共享同一套内存）。
- [ ] 用户态与内核态的阶级森严：Ring 3 到 Ring 0 的物理权限切换。为什么系统调用（System Call）极其昂贵？
- [ ] 上下文切换（Context Switch）的物理代价：线程切换到底在干什么？（保存 CPU 寄存器、切程序计数器等现场）。
- [ ] 锁的底层歧视链：自旋锁（Spinlock，用户态死循环傻等，快！） vs 互斥锁（Mutex，拿不到锁就触发系统调用，交出 CPU 控制权去 OS 队列里休眠，极慢！）。

## 🌐 境界四：洞虚 —— 游戏网络物理传输（CN 协议篇）
**师兄批注**：别去背什么 HTTP 请求头，游戏客户端只关心数据包怎么最快地砸到服务器脸上！

- [ ] TCP 的物理底裤：面向连接（三次握手证明收发能力）、绝对可靠（超时重传）、保证顺序。
- [ ] UDP 的大喇叭哲学：无连接、不可靠、不管顺序、甚至包头极小。为什么它的延迟极其低？
- [ ] TCP 的灾难 1：Nagle 算法（指令粘包）：TCP 为什么会扣下你的小技能按键，等凑够一个大包才发？怎么用 TCP_NODELAY 关掉它？
- [ ] TCP 的灾难 2：队头阻塞（Head-of-line Blocking）：第一帧的包丢了，为什么 TCP 会把已经收到的第二帧、第三帧死死捂住不给游戏引擎？（导致游戏画面瞬移的元凶）。

## 🎮 境界五：大乘 —— 游戏专属网络架构（CN 进阶篇）
**师兄批注**：这是区分“懂点网络”和“真正做过游戏”的绝对分水岭！大厂必问！

- [ ] KCP / 可靠 UDP 的降维打击：底层用 UDP 无脑狂发保证极低延迟，上层（应用层 C++ 代码）自己加序号和 ACK 确认。丢包了自己立刻重传，彻底绕开操作系统的黑盒！
- [ ] 帧同步（Lockstep）：只发玩家的操作指令（比如“向前走”），所有客户端依靠确定的物理引擎各自推演。为什么极其容易出现“蝴蝶效应”导致不同步？为什么不能用浮点数算逻辑？
- [ ] 状态同步（State Sync）：服务器是唯一真神，服务器跑完全部逻辑后，直接把坐标和血量广播给客户端。客户端只负责无脑播动画（大部分 MMORPG 的做法）。
- [ ] 网络发包序列化：把 C++ 的结构体扔进网线前，为什么要用 htonl() 转换大小端？为什么要考虑字节对齐导致的无效流量填充？

## 🏗️ 境界六：登仙 —— 游戏架构与设计模式 (核心增补！)
**师兄批注**：这是考察你是不是一个“正规军”游戏开发者的关键！写游戏绝不是想到哪写到哪。

- [ ] 游戏主循环 (Game Loop) 与 Delta Time：为什么游戏必须有一个死循环？为什么所有的移动逻辑都必须乘以 deltaTime（帧间隔时间）？
- [ ] 状态机 (FSM / Finite State Machine)：怎么管理角色的“站立、走、跑、跳、攻击”状态流转，避免满篇的 if-else 灾难？
- [ ] 单例模式 (Singleton)：为什么大厂主程既爱用单例（管理全局的 GameManager / AudioManager），又痛恨单例（多线程读写灾难、生命周期混乱）？
- [ ] 观察者模式 (Observer / Event System)：当玩家血量降低时，怎么做到让 UI 自动更新、成就系统自动判定，而不需要把这些代码全塞进玩家受伤的函数里？
- [ ] 空间划分数据结构 (你的简历杀器)：QuadTree（四叉树，2D）、Octree（八叉树，3D）、BVH（层次包围盒）。它们是怎么把碰撞检测从 O(N^2) 降维到 O(N log N) 的？

---

# 🗺️ 游戏客户端 C++ 终极天赋树（打勾自检清单）
## 🧱 境界一：筑基 —— 物理内存与指针微操（绝对底盘）
**师兄批注**：这是 C++ 的尊严。这里答错任何一题，面试官会认为你根本不懂计算机，直接一票否决！

- [ ] 五大内存分区：堆、栈、全局/静态、常量、代码区的物理区别（谁来分配？谁来销毁？）。
- [ ] 指针与引用：引用的底层本质（T* const），传值、传指针、传引用的物理代价与选择。
- [ ] 内存分配：new/delete 与 malloc/free 的本质区别（是否调用构造/析构函数？）。
- [ ] 内存对齐（Byte Alignment）：struct 和 class 在内存里到底占几个字节？为什么要填充空白字节（CPU Cache 命中率）？
- [ ] 内存泄漏与野指针：什么是内存泄漏（风筝断线）？什么是野指针/悬垂引用（死人墓地）？
- [ ] const 的四大用法：修饰变量（只读）、修饰指针（顶层 const 指针不可变，底层 const 指向的值不可变）、修饰函数参数（防止别人乱改）、修饰成员函数（承诺绝对不修改类的成员变量）。
- [ ] static 的三条命：隐藏在函数内部的 static（寿命等于程序寿命，但只有这个函数认识它）、类里的 static 成员变量（全家共享一份，必须在类外初始化）、限制作用域的 static 全局变量（只能在当前 cpp 文件里用，别的 cpp 文件看不见）。

## 🧬 境界二：结丹 —— 面向对象与多态底层（大厂主程最爱）
**师兄批注**：游戏里有成千上万的实体（Entity/Player/Enemy），全靠面向对象来组织。不懂虚表，你看引擎源码就像看天书！

- [ ] 三大特性：封装、继承、多态的工程意义。
- [ ] 多态的物理真相（重中之重）：虚函数表（vtable）和虚指针（vptr）的内存布局。
- [ ] 生死时序：为什么基类的析构函数必须是虚函数？为什么构造函数绝对不能是虚函数？
- [ ] 同名隐藏机制：重载（Overload）、重写（Override）、隐藏（Hide）的严格区分。
- [ ] 四大类型转换：static_cast（安全免责）、dynamic_cast（RTTI 性能灾难）、reinterpret_cast（指鹿为马）、const_cast（暴力破窗）。

## 🧙‍♂️ 境界三：化神 —— 现代 C++ (C++11/14) 魔法机制
**师兄批注**：老 C++ 程序员的试金石。我们刚才血战的阵地全在这里！

- [ ] 移动语义：左值与右值的物理界限，std::move 的本质（无条件转右值，贴流浪汉标签），将亡值劫持。
- [ ] 完美转发：std::forward 如何打破“有了名字就变左值”的魔咒。
- [ ] 智能指针：shared_ptr 的引用计数原理，循环引用的死局，weak_ptr 如何破局，unique_ptr 的独占权。
- [ ] Lambda 表达式：[=] 值捕获（复印件与 mutable）、[&] 引用捕获（悬垂引用崩溃的灾难）。
- [ ] 其他神兵利器：auto 类型推导、nullptr（取代宏定义 NULL）、constexpr（编译期算值）。

## 🏭 境界四：洞虚 —— STL 工业级源码盘问
**师兄批注**：你每天都在用的数据结构，面试官会直接让你默写它们底层的物理构造！

- [ ] std::vector：底层三指针（_Myfirst, _Mylast, _Myend），扩容机制（1.5倍/2倍），push_back vs emplace_back 的物理区别。
- [ ] 关联容器大乱斗：map/set（红黑树：自动排序，O(logN)） 对决 unordered_map/set（哈希表：极速单点查询，O(1)，哈希冲突）。
- [ ] 迭代器失效（极其高频的写 Bug 现场）：在一个 for 循环里遍历 vector 并 erase 掉某个元素，为什么程序会当场崩溃？应该怎么写？
- [ ] std::string：底层的 SSO（短字符串优化）机制是什么？

## ⚙️ 境界五：前置真理 —— C++ 程序的降生
**师兄批注**：这是 C++ 独有的浪漫！面试官必问：“你点下 Build 按钮后，电脑到底干了什么？”

- [ ] 预处理（Preprocessing）：把所有的 #include 里的代码无脑复制粘贴过来，把 #define 宏全部替换掉（纯文本替换，不查语法）。
- [ ] 编译（Compilation）：把 C++ 代码翻译成汇编代码，检查语法错误。
- [ ] 汇编（Assembly）：把汇编代码变成机器看得懂的二进制文件（.obj 或 .o）。
- [ ] 链接（Linking）：把一堆零散的 .obj 文件，加上调用的第三方库，全部用胶水缝合在一起，变成一个完整的 .exe 执行文件！（如果函数只写了声明，没写实现，就在这一步报错无法解析）。

---

# 🗺️ 游戏客户端/引擎：计算机图形学 (CG) 终极天赋树（打勾自检清单）

## 🎨 境界一：筑基 —— 渲染流水线 (The Graphics Pipeline)
**师兄批注**：这是图形学的“九年义务教育”！面试官通常开局第一个问题就是：“请你描述一下渲染管线的完整流程。” 

- [ ] 三大阶段物理宏观：应用阶段（CPU 准备数据，发 DrawCall）、几何阶段（GPU 处理顶点）、光栅化阶段（GPU 画像素）。
- [ ] 几何阶段细分：顶点着色器（Vertex Shader，必备） -> 曲面细分/几何着色器（可选） -> 裁剪（Clipping） -> 屏幕映射（Screen Mapping）。
- [ ] 光栅化阶段细分：三角形设置 -> 三角形遍历（生成片元 Fragment） -> 片元着色器（Fragment/Pixel Shader） -> 逐片元操作（深度测试 Z-Test、混合 Blending）。

## 📐 境界二：结丹 —— 3D 数学与 MVP 矩阵 (简历高频雷区)
**师兄批注**：既然你手搓了 SoftRenderer，面试官一定会让你在白板上推导矩阵！

- [ ] 向量双子星：点乘（Dot Product，算夹角、算光照衰减）与叉乘（Cross Product，算平面的法线、判断点是否在三角形内部，你简历里写了！）。
- [ ] 齐次坐标 (Homogeneous Coordinates)：为什么 3D 空间里的变换非要用 4x4 的矩阵？（为了把“平移”操作也统一成矩阵乘法，区分点 w=1 和向量 w=0）。
- [ ] MVP 矩阵的物理意义：M (Model) 把模型从自己的局部空间放到世界空间；V (View) 把世界空间的所有物体，搬到摄像机的面前；P (Projection) 极其变态，透视投影的本质是“近大远小”，它把视锥体压成一个标准设备坐标 (NDC) 的正方体。
- [ ] 四元数 (Quaternion)：为什么旋转不用欧拉角（会产生 Gimbal Lock 万向节死锁），而要用四元数插值（Slerp）？

## 🖌️ 境界三：化神 —— 软光栅底层物理 (你的绝对主场)
**师兄批注**：这块完全是针对你简历上“SoftRenderer”量身定制的夺舍拷问！

- [ ] Bresenham 画线算法：怎么只用整数的加减法，不沾一滴浮点数，就能画出一条平滑的直线？
- [ ] 三角形遍历：AABB 包围盒是怎么圈定像素范围的？怎么利用向量叉乘判断一个像素点到底在不在三角形里？
- [ ] 重心坐标 (Barycentric Coordinates)：这玩意不仅能判断点在不在三角形内，最核心的作用是插值！顶点的颜色、法线、UV 坐标，是怎么通过重心坐标平滑过渡给内部每一个像素的？
- [ ] Z-Buffer (深度测试)：为什么不用画家算法（从后往前画）？Z-Buffer 怎么完美解决物理遮挡？什么是 Z-Fighting（深度冲突，远处两个紧贴的面疯狂闪烁），怎么解决？

## 💡 境界四：洞虚 —— 光照与材质 (基础 Shading)
**师兄批注**：你的软光栅可能没写这部分，但面试官极大概率会顺口问一句作为拓展。

- [ ] Blinn-Phong 反射模型：三大金刚 —— 环境光 (Ambient) + 漫反射 (Diffuse，光向量与法线的点乘) + 高光 (Specular，半程向量与法线的点乘)。
- [ ] 着色频率：Flat Shading（按面着色，有棱角）、Gouraud Shading（按顶点着色，插值颜色）、Phong Shading（按像素着色，插值法线，最平滑）。

## ✨ 境界五：大乘 —— 纹理与后处理 (结合 2D 弹幕项目)
**师兄批注**：你在《星海先锋》里写了 Bloom 泛光和 FBO，这证明你已经触碰到了现代游戏画面的命脉！

- [ ] 纹理映射 (Texture Mapping)：UV 坐标是什么？当贴图比屏幕像素小（放大）或大（缩小）时，双线性插值 (Bilinear) 和 Mipmap 分级纹理是怎么解决锯齿和摩尔纹的？
- [ ] FBO (Frame Buffer Object) / 离屏渲染：基于 FBO 离屏渲染结合基础着色器实现全屏泛光，绝对会被问！物理动作是什么？（先不画到屏幕上，先画到内存的一张纹理图上，然后对这张图做特效，最后贴到屏幕上）。
- [ ] Bloom (泛光) 的物理过程：提取画面高光区域 -> 高斯模糊 (Gaussian Blur) 晕开 -> 叠加 (Additive Blend) 回原图。