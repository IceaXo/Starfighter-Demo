# Starfighter: The Void Vanguard (星海先锋)

> 一个基于 C++11 和 Raylib 的高性能 2D 弹幕射击游戏演示 (Tech Demo)。
> 专为展示 **现代 C++ 内存管理 (RAII)**、**对象池架构 (Object Pooling)** 以及 **数据驱动设计** 而设计。

## 🎮 核心特性 (Key Features)

### 1. 🚀 高性能对象池 (Object Pooling) [v0.2 New!]
* **痛点解决**：摒弃了传统的 `new/delete` 动态内存分配，解决了高频创建/销毁子弹和敌人导致的内存碎片化与 GC 震荡问题。
* **实现细节**：
    * **双重池化**：实现了 `Enemy` (容量100) 和 `Bullet` (容量30) 的双重对象池。
    * **预分配内存**：使用 `std::vector::reserve` 锁定内存空间，杜绝运行时扩容。
    * **O(1) 开销**：通过 `active` 标记位复用“死亡”对象，实现零运行时分配 (Zero Runtime Allocation)。

### 2. 📈 动态难度曲线 (Dynamic Difficulty) [v0.2 New!]
* **天道算法**：游戏难度随玩家分数线性增长，拒绝枯燥的重复劳动。
* **多维压制**：
    * **生成频率**：基于 `Score / 500` 的衰减公式，敌人生成间隔从 60 帧平滑压缩至 20 帧。
    * **移动速度**：基于 `Score / 100` 的增量公式，敌人速度无上限提升。

### 3. 🛠️ 工业级架构 (Entity Architecture & FSM)
* **状态机 (State Machine)**：引入 `GameState` 管理 `PLAYING` / `VICTORY` / `GAME_OVER` 状态流转，解耦物理与渲染。
* **实体组件**：利用 C++ **多态** 与 **虚函数** 实现 `Entity` 体系的统一管理。
* **配置驱动**：构建 `GameConfig` 命名空间，将所有硬编码参数提取为 `constexpr` 编译期常量。

## 🛠️ 技术栈
* **语言**: C++ 11/14 (GCC MinGW)
* **图形库**: Raylib 4.5
* **工具**: VS Code, Git

---

## 📅 开发日志 (Dev Log)

### 📅 [2026-2-18] 阶段三：元婴·高性能池化与逻辑闭环
> **摘要**：针对性能瓶颈进行底层重构，实装**双重对象池**与**状态机**，完善生存机制。

#### ✅ 今日完成 (Completed)
1.  **双重对象池 (Dual Object Pooling)**：
    - **Bullet Pool**：重构射击逻辑，实装 30 发容量的环形子弹池，将射击开销降至 $O(1)$。
    - **Enemy Pool**：完善敌人池化逻辑，杜绝刷怪时的堆内存分配。
2.  **状态机与流程 (Game Loop)**：
    - 实装 `enum class GameState`，实现完整的游戏胜负判定流程。
    - 实现了 `HP` 血量系统 (HP=5) 与死亡/胜利结算画面。
3.  **生存机制优化 (Survival)**：
    - 实现了 **无敌帧 (I-Frames)** 逻辑：受击后 0.5 秒免疫伤害。
    - 实现了基于 `GetTime() % 2` 算法的高频闪烁视觉反馈。
4.  **Bug 修复**：
    - 修复了“子弹击杀敌人后，同一帧内敌人仍能对玩家造成碰撞伤害”的逻辑漏洞。

---

### 📅 [2026-2-17] 阶段二：结丹·数据驱动与数值平衡
> **摘要**：引入**数据驱动**思想消除硬编码，并实装**动态难度算法**。

#### ✅ 今日完成 (Completed)
1.  **天道法则 (Config-Driven)**：
    - 构建 `GameConfig` 命名空间，将窗口大小、生成频率等魔术数字提取为 `constexpr` 编译期常量。
2.  **动态难度 (Dynamic Difficulty)**：
    - 实装线性反馈算法：生成频率随分数增加而加快 (60帧 -> 20帧)。
    - 实装速度增量算法：敌人飞行速度随分数无限递增。

---

### 📅 [2026-2-16] 阶段一：筑基·架构重构与物理基础
> **摘要**：完成从“单文件脚本”到“面向对象架构”的转型。

#### ✅ 今日完成 (Completed)
1.  **架构重构 (Refactoring)**：
    - 创建基类 `Entity`，实现属性复用。
    - 将 `Player`、`Bullet`、`Enemy` 拆分到独立文件 (.h / .cpp)。
    - 实现了 `vector` 基础内存管理。
2.  **核心机制 (Mechanics)**：
    - 实现了 `WASD` 移动与 `Space` 射击。
    - 实现了基于 Raylib `CheckCollisionCircles` 的碰撞检测。
    - 实现了敌人死亡后的无限重生逻辑。

---

## 🚧 待办事项 (Roadmap)
1.  **[P0] 图形学特效**：实现粒子系统 (Particle System)，为爆炸添加碎片与光效。
2.  **[P1] 着色器编程**：编写 GLSL Shader 实现子弹泛光 (Bloom) 与背景滚动。
3.  **[P2] 空间优化**：引入四叉树 (QuadTree) 优化大规模碰撞检测性能。